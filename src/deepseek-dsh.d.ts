/** Internal executable entry exported by the published DeepSeek Harness CLI artifact. */
declare module '@deepseek-ai/dsh/lib/bin.js' {
  /** Run the DeepSeek Harness CLI against the current process arguments. */
  export function runCli(): Promise<void>
}
